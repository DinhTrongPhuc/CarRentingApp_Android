<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".CarRentingApplication"
        android:theme="@style/Theme.CarRentingApp"
        android:usesCleartextTraffic="true">

        <!-- Splash / Launcher -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CarRentingApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".activities.LoginActivity" android:exported="false"/>
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>

        <!-- Main -->
        <activity android:name=".activities.MainActivity" android:exported="false"/>

        <!-- Car Detail -->
        <activity android:name=".activities.CarDetailActivity" android:exported="false"/>

        <!-- Post Car -->
        <activity android:name=".activities.PostCarActivity" android:exported="false"/>

        <!-- Edit Car -->
        <activity android:name=".activities.EditCarActivity" android:exported="false"/>

        <!-- Booking -->
        <activity android:name=".activities.BookingActivity" android:exported="false"/>

        <!-- Payment -->
        <activity android:name=".activities.PaymentActivity" android:exported="false"/>

        <!-- Profile -->
        <activity android:name=".activities.ProfileActivity" android:exported="false"/>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDCWg5reP3WbgxfHqPV3yFJy3eWK72hXsY" />

    </application>
</manifest>
